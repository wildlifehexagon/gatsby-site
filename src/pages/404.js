import React from 'react'
import Layout from '../components/layout'

const NotFoundPage = () => (
  <Layout>
    <h1>PAGE NOT FOUND</h1>
    <p>This page doesn't exist. :(</p>
    <p>Try a link above instead.</p>
  </Layout>
)

export default NotFoundPage
